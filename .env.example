# Docker/Production Environment Variables

# JWT Secret for Authentication Service
# Change this to a secure random string (minimum 256 bits)
JWT_SECRET=your-super-secret-key-change-this-in-production-min-256-bits

# Frontend API Base URL Configuration
# Set this based on your environment

# WITH NGINX RUNNING (Docker):
# Use localhost when FE is running outside Docker but nginx is in Docker
VITE_API_BASE_URL=http://localhost

# WITHOUT NGINX (Local development only):
# VITE_API_BASE_URL=http://localhost:8082

# If FE is running inside Docker:
# VITE_API_BASE_URL=http://nginx

# Note: Docker-specific environment variables (PostgreSQL, ports, etc.) are configured in docker/.env
# See docker/.env.example for Docker service configuration

